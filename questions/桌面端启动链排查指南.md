# 桌面端启动链排查指南

## 问题描述

最终便携版已无 preload ESM 语法错误，但在隔离用户目录启动 15 秒后，Electron wrapper 和 renderer 进程存在，`OPENCODEX_HOME` 为空，未出现 `runtime-port.json`，也未发现 healthz。内置 Bun sidecar 单独从 `out/win-unpacked/resources/opencodex` 启动时可以写 ready 文件并返回 healthz 200，因此问题范围缩小到便携版解包后的 Electron → supervisor → Bun 启动链。

## 已尝试的修复方法及失败原因

- ❌ 2026-08-01：启动最新签名便携版，设置唯一 `OPENCODEX_HOME`、`CODEX_HOME`、`--user-data-dir`、`--no-sandbox`，等待 30 秒仍无 runtime-port；只观察到临时目录中的 OpenCodex Electron 子进程。
- ✅ 2026-08-01：直接启动同一 unpacked 资源中的 `runtime/bun.exe src/desktop/entry.ts`，取得 ready JSON，`/healthz` 返回 200；证明 sidecar 资源、动态端口和服务本身正常。
- ❌ 2026-08-01：便携版加 `--enable-logging=file --log-file=...` 后重试，15 秒内临时根和隔离 home 仍为空，未生成 Electron 日志；未把“进程存在”当作启动成功。
- ❌ 2026-08-01：应用 `--backend=copyfile` 并重新签名后，便携版隔离 smoke 仍在 30 秒内未写 `runtime-port.json`；wrapper/renderer 进程存在但没有 Bun ready，说明硬链接问题修复后仍有 Electron supervisor 启动链问题。
- ❌ 2026-08-01：检查 copyfile 版本的便携解包资源，发现 `resources/opencodex/node_modules` 整个目录未被 electron-builder 的 extraResources 复制；独立 Bun 因缺少 `path-key` 仍回退到开发机缓存并失败。根因进一步缩小到 electron-builder 对 extraResources 源根 `node_modules` 的默认过滤。
- ✅ 2026-08-01：将 staging `node_modules` 作为独立 extraResource 源复制到 `opencodex/node_modules`，并过滤不需要的 `bun`/`@oven` 包；离线本地 Electron 构建成功，便携解包资源存在 `node_modules/path-key`。最终签名便携版隔离启动取得 `runtime-port.json`，`/healthz=200`、`/v1/models=200`；临时根已在补停子进程后清理。

## 深层问题分析

当前证据表明 preload 格式问题已解决，sidecar 本身也能运行；剩余问题可能是便携 wrapper 解包后传递环境变量/资源路径、Electron 主进程启动 supervisor 时的 spawn 错误未输出，或桌面宿主在无交互桌面会话中未完成 `app.whenReady` 后续逻辑。需要捕获主进程的 supervisor 错误或在真实交互桌面复测。

## 下一步排查策略

1. 在便携解包临时根内定位实际 `bun.exe` 和 `entry.ts`，独立启动并比较环境变量与工作目录。
2. 为桌面宿主增加仅面向诊断的错误可观测性前先评估是否属于产品行为变更；优先使用 Electron 日志或真实桌面会话。
3. 在 Windows 10/11 交互桌面 VM 双击便携版，观察 tray、Dashboard、runtime-port 和 healthz。

## 调试工具

- `Get-Process` 按临时解包路径过滤 Electron/Bun 子进程
- 隔离 `OPENCODEX_HOME`、`CODEX_HOME`、`--user-data-dir`
- `runtime-port.json`、sidecar stdout/stderr 重定向
- `Get-ChildItem` 检查便携解包资源树

## 注意事项

- 不要修改 Provider、服务器或启动链来掩盖当前无交互会话限制。
- 每次 smoke 必须停止 wrapper 及其临时解包子进程，再清理唯一临时根。
- 只有真实 runtime-port、healthz 和模型请求结果才能标记便携版可用。

## 更新记录

- 2026-08-01：记录便携版 Electron 启动链未取得 sidecar ready，待交互桌面/诊断日志复核。
- ❌ 2026-08-01：本轮真实便携版冒烟已取得 `runtime-port.json`、`/healthz=200` 与 `/v1/models=200`，但首次用 `CloseMainWindow()` 结束进程时只触发窗口隐藏，未触发应用退出；随后直接删除隔离用户数据目录时，Electron/Chromium cache 与 LevelDB 文件仍被占用，`Remove-Item` 报文件正在使用。下一步必须先按可核验路径确认本轮 OpenCodex 主进程及其子进程，再做精确 PID 停止和目录清理，不能使用广泛 `taskkill`。
- ✅ 2026-08-01：按主进程命令行和资源路径核验到 PID 45216（主进程）、26064/39572/46776（Electron 子进程）及 39528（内置 Bun sidecar），全部属于本轮 `opencodex-portable-smoke-route-20260801` 隔离目录；精确停止后删除该目录成功，`Test-Path` 确认 `False`。说明冒烟清理必须处理窗口隐藏语义，并按父子进程精确回收。
- ❌ 2026-08-01：阶段 21 首次用 `Start-Process` 启动隔离 CLI proxy 的外层编排命令返回 1 且未回传 JSON；随后读取精确 stdout/stderr 发现 CLI 已在 36820 正常监听并完成隔离 Codex 注入，日志文件最初为空只是重定向刷新时序。后续以 `runtime-port.json`、healthz 和 PID 为准继续，不重复启动第二个 CLI proxy。
- ❌ 2026-08-01：阶段 21 在默认沙箱中读取 `C:\tmp\opencodex-stage21-pages-20260801` 的隔离 runtime 文件时被 `Access is denied` 拒绝。该路径只包含本轮隔离测试数据；后续改用最小范围授权进行只读核验，绝不读取或修改用户当前 Codex 的真实配置与进程。
- ❌ 2026-08-01：阶段 21 真实启动隔离桌面端时，Dashboard 正确复用了已有 CLI proxy 的 PID 3548 与端口 36820，healthz=200，但 Electron 主进程仍派生了内置 `resources/opencodex/runtime/bun.exe .../src/desktop/entry.ts`（PID 41252）。虽然 runtime 未漂移，也未观察到第二监听端口，这仍违反“已有 CLI proxy 不启动第二 sidecar”的资源与所有权边界；必须修正桌面主进程启动前的存活复用判定并补回归测试。
- ❌ 2026-08-01：在 `desktop` 子目录执行类型检查时误用了相对命令 `node_modules\bun\bin\bun.exe`，PowerShell 将其解析为模块名并立即失败；仓库 Bun 实际位于上级目录，后续固定使用 `..\node_modules\bun\bin\bun.exe`，不重复错误路径。
- ❌ 2026-08-01：补齐桌面 sidecar 生命周期后，桌面测试 17/18 通过，唯一失败是 `lifecycle-static.test.ts` 仍硬编码旧的单行 import 文本；实现已改为多行导入更多生命周期依赖，行为本身未失败。下一步把脆弱字符串断言改成关键符号/失败关闭语义断言，再重跑全部桌面测试。
- ❌ 2026-08-01：新增隔离停机集成测试首轮发现，成功恢复并 drain 后只设置 `process.exitCode`，但 Electron 持有的 stdin 管道仍打开，sidecar 不会自然退出；测试超时后又未等待强制终止完成便清理临时目录，引发 `EACCES`。修复方向是成功清理后显式 `process.exit(exitCode)`，测试端等待子进程确实退出再删隔离目录。
- ❌ 2026-08-01：隔离停机集成测试改为显式退出后，Bun 在默认沙箱对 `C:\tmp` 的 `mkdtemp` 返回 `EPERM`；失败发生在 sidecar 启动前，没有遗留进程或目录。后续把测试隔离根改到仓库已忽略且可写的 `.tmp/`，避免请求不必要的系统临时目录权限。
- ❌ 2026-08-01：将隔离根移入仓库 `.tmp/` 后，API 动态端口测试 8/8 通过、桌面生命周期静态测试 6/6 通过，但真实 stop-refused 集成测试在 30 秒总时限超时。下一步读取该次 stdout/stderr 与具体等待阶段，压缩启动依赖或把总时限调整为覆盖 Windows 冷启动，同时仍要求每个状态等待有独立上限，不能用无限等待掩盖问题。
- ❌ 2026-08-01：扩展运行 system-env、token-guardian、Desktop 3P 相关既有测试时，managed sandbox 对 `%TEMP%` 的目录扫描/清理触发 24 个 `EPERM/EACCES`（含 config-ownership scandir 被拒）；聚焦生命周期 6/6、desktop-ready、Grok lifecycle 与 typecheck 均通过，证据表明是测试临时目录 ACL 限制而非本次 entry 行为。后续优先使用仓库 `.tmp/` 的聚焦回归，扩展组留到非沙箱/CI 复核。
- ❌ 2026-08-01：将停机逻辑抽成可行为测试的事务函数后，桌面套件再次为 17/18，通过项含新的外部复用/失活监控；唯一失败仍是旧静态测试要求 `if (ownsServer && codexRoutingOwned)` 和 `const restored = restoreNativeCodex()` 的原位源码形状。实现已变为 `state.*` 与注入 action，下一步同步静态合同到新边界，行为由 root 侧真实事务测试兜底。
- ❌ 2026-08-01：最终包的桌面自有代理已取得动态端口 18646、healthz=200 并把隔离 Codex 指向该端口，但直接无凭据读取 `/api/keys` 被管理面正确返回 `opencodex admin token required`，因此本次 `ApiBaseUrl=null` 不能判断动态地址失败。下一步只从本轮隔离 `OPENCODEX_HOME` 读取受保护 admin token，并带 `X-OpenCodex-API-Key` 重试；不降低管理面认证。
- ❌ 2026-08-01：首次真实 stop-refused 桌面验收中，PowerShell 内联 `Add-Content` 的双引号转义被外层解析破坏，测试用冲突块没有写入；命令因非终止错误仍继续点击停止，代理按正常路径退出并删除 runtime，所以该轮没有触发拒绝语义。下一次先用 `Set-Content`/已验证内容写入后回读确认冲突块存在，再点击停止；不把正常停机误记为失败关闭通过。
- ❌ 2026-08-01：第二次 stop-refused 真实验收已确认冲突文本写入，但所用“native subagent default”附加块被当前恢复逻辑判定为可安全移除，代理仍正常停止并删除 runtime；该夹具没有制造恢复拒绝。下一次先从现有 Codex journal/inject 回归中查找真实的歧义冲突样例，不能继续猜测标记形状。
- ❌ 2026-08-01：查找真实恢复冲突夹具时把 `tests\codex-*` 等通配符直接作为 Windows `rg` 路径参数，触发路径语法错误且未执行搜索。后续固定在 `tests` 目录搜索并用 `-g 'codex-*.test.ts'` 等 glob 过滤，不再把通配符放进路径。
- ❌ 2026-08-01：按现有 journal 回归准备真实冲突夹具前，尝试从 PID 46632 的离线页查找“启动代理”按钮失败；可能仍有上一轮 Chromium 对话框/页面切换状态，尚未执行启动或冲突测试。下一步先只读枚举该窗口当前按钮与进程状态，确认 UI 所处状态后再操作，不盲目发送按键。
- ❌ 2026-08-01：阶段 21 临时目录首次精确清理时，PowerShell 构造两个允许根的数组表达式不符合预期，安全检查把合法的 `C:\tmp\opencodex-stage21-*` 也拒绝；命令在任何删除前终止。下一次逐目标用明确的 `C:\tmp\` 或仓库 `.tmp\` 前缀校验，不放宽到宽泛目录，也不重复错误数组表达式。
- ✅ 2026-08-01：Electron supervisor 在 spawn 前直接读取 runtime/config 并执行 opencodex `/healthz` 身份探测；最终包复用隔离 CLI 的 PID 3548、端口 36820、healthz=200，Electron 只有 3 个自身子进程，`BunChildCount=0`。精确关闭桌面主进程及子进程后，CLI 仍 healthz=200；随后用同一隔离 home 正常 `ocx stop`，CLI 退出、runtime 删除、Codex 恢复为原生配置。
- ✅ 2026-08-01：外部 ownership 已进入 `BackendStatus`，Dashboard 实际显示禁用按钮“由外部 CLI 管理”；外部代理有周期探活，连续失活会向 Electron 报 failed 并进入既有有界恢复。桌面行为测试覆盖直接复用不 spawn 与外部失活两条路径。
- ✅ 2026-08-01：桌面自有 sidecar 已补齐普通 `ocx start` 的 journal、catalog prewarm、crash guard、token/history guardian、system env、Desktop 3P 与 Grok 生命周期；API、Claude、settings DTO 均使用请求所到达的实际动态端口。最终包实测端口 18646，三类返回端口均为 18646。
- ✅ 2026-08-01：停止恢复改为事务式失败关闭。最终包在隔离 Codex 中按既有 journal 回归制造“受管 subagent marker 与值之间多一个空行”的真实冲突后点击停止：runtime 仍存在、sidecar/Desktop 均存活、healthz=200；修复隔离夹具后第二次停止成功，runtime 删除、配置恢复、sidecar/Desktop 均退出。即恢复有歧义也不会先杀代理留下死路由。
- ✅ 2026-08-01：正常停止后的本地离线页提供“启动代理/退出”按钮；实测停止后 Codex 配置恢复，同一窗口点击启动得到新 PID/动态端口并重新同步，第二次停止与退出无进程残留。阶段 21 所有 C:\tmp/仓库 `.tmp` 隔离目录及一次性 PFX/私钥证书均已精确清理。
- ❌ 2026-08-01：新增“stopped ack + exit 0”握手回归后，crash fixture 按预期被拒绝，但 graceful fixture 在 5 秒内未退出并被测试运行器终止；原因是 fixture 只监听一次 stdin `data`，Bun/Windows 管道可能先送达空白或分片，且测试默认 5 秒上限与 supervisor 自身 5 秒拒绝门重合。下一步让 fixture 像真实 sidecar 一样累积行缓冲并只在收到完整 `stop\n` 后确认，测试本身使用略高于产品门的有界 timeout；不放宽产品的 5 秒停机确认。
- ✅ 2026-08-01：fixture 改为与真实 sidecar 相同的逐行缓冲并显式 `resume()`，测试上限仅提高到 10 秒、产品停机确认仍为 5 秒。desktop typecheck 通过；握手与异常退出聚焦回归 8/8、63 个断言通过，其中 graceful 必须同时输出 `stopped` 且 exit 0，crash fixture 的 exit 1 会被 supervisor 拒绝并标记 failed。root typecheck 与 shutdown transaction 7/7、42 个断言亦通过。
- ❌ 2026-08-01：最终 diff 自审发现 shutdown transaction 的初版 `finally` 会在 `drainServer()` 抛错时仍执行 `confirmStop()` 并沿用原 exit code；这会把未完成 drain 的异常路径误标为安全停止，虽然现有正常 smoke 不触发。修复必须用独立 `safelyStopped` 提交标志，仅在 drain 与 runtime cleanup 都成功后输出确认；异常路径只做 best-effort cleanup 并 exit 1。
- ✅ 2026-08-01：shutdown transaction 已加入 `safelyStopped` 提交标志；只有 drain 与 runtime cleanup 都成功才输出 `stopped`，否则不确认并固定 exit 1，Electron 会把它视为 failed 后有界恢复。新增 drain 抛错行为回归验证调用序列不含 `confirm-stop`；root typecheck 与 8/8、44 个断言通过，desktop typecheck 与 supervisor 4/4、10 个断言通过。
- ❌ 2026-08-01：最终多 Agent 只读审查发现停止仍有 TOCTOU：当前先 `restoreNativeCodex()`、后 `drainAndShutdown()`，而 management `/api/sync` 不属于 data turn drain；已进入或两步间进入的同步可能在恢复后重新注入动态端口，再由 sidecar ack+exit 0 留下死路由。同时 integration cleanup 吞掉 Grok/system-env 失败，外部 owner 的托盘停止/重启语义仍会误导，非 loopback external hostname 也会被探活后固定用 127.0.0.1 加载。下一步建立可测试的停止写入闸门和在途同步等待，把最终恢复放在闸门之后；integration 失败不得确认，托盘按 ownership 拒绝错误操作，外部复用只接受实际 loopback 地址。
- ✅ 2026-08-01：新增进程级 Codex sync shutdown gate：先原子阻止新同步、等待已进入同步归零，再清理 integration 与恢复 Codex；同步超时、Grok/system-env 清理失败、Codex 恢复失败均解除闸门并保持 proxy/listener/runtime 在线，drain/runtime cleanup 失败则无 `stopped` ack 且 exit 1。同步 gate/事务回归 23/23、105 个断言通过；外部托盘 stop/restart 按 ownership 禁用，LAN/纯 IPv6 external hostname 被 loopback 兼容门拒绝，desktop 21/21、116 个断言通过。
- ❌ 2026-08-01：最终四路并行回归时，desktop graceful fixture 在整个测试 10 秒上限内尚未完成冷启动/ready，被 Bun 测试运行器 SIGTERM 并报告 20 pass/1 fail；同一 fixture 此前串行已多次通过，产品 supervisor 的 5 秒 stop ack 门只在 ready 后开始，未被本次失败触发。下一步只把测试总上限提高到 30 秒以容纳并行冷启动，产品停机门保持 5 秒，并串行复跑 desktop 全套；不修改握手业务逻辑。
- ✅ 2026-08-01：仅将两个真实子进程 fixture 的测试总上限改为 30 秒，supervisor 产品 stop ack 门仍为 5 秒；串行 desktop typecheck 与全套 21/21、116 个断言通过，graceful/crash fixture 分别约 4.6/4.5 秒完成，无 dangling process。
- ❌ 2026-08-01：修复后最终复审确认 sync TOCTOU 已关闭，但发现 sidecar 内仍保留第二套 unrestricted `findLiveProxy()` fallback：Electron 拒绝 LAN/纯 IPv6 后，sidecar 仍可能接管并被 supervisor 错标为 desktop owner；同时 foreign service 环境下桌面启动会照常注入 system-env/Grok，退出却因 `serviceEnvironmentOwnedHere()` 为 false 跳过清理。下一步删除 sidecar 的外部接管分支，让 Electron 成为唯一 ownership authority；启动时捕获共享 integration ownership，仅在归当前环境时注入，并按本轮实际 ownership 严格清理。
- ✅ 2026-08-01：删除 sidecar 的 `findLiveProxy()` 外部接管分支后，Electron supervisor 成为唯一外部探活、loopback 兼容性和 ownership 判定权威；`entry.ts` 在启动时捕获 `integrationEnvironmentOwned`，仅在当前环境归桌面所有时写入 system-env/Grok，并以 `grokRoutingOwned` 只清理本轮实际变更。静态生命周期回归明确禁止 sidecar 再导入/调用 `findLiveProxy`，root 相关回归 23/23、107 个断言及 desktop 全套 21/21、116 个断言通过；最终签名包的隔离 UI smoke 在动态端口 36666 完成 stop ack、路由恢复、sidecar 退出与离线退出。
