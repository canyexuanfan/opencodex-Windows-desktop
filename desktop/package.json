{
  "name": "@bitkyc08/opencodex-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "OpenCodex Windows desktop host",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test tests",
    "package:resources": "bun ../scripts/prepare-desktop-resources.ts",
    "package": "bun run package:resources && bun run build && electron-builder"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.3.14",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "24.12.3",
    "electron": "43.2.0",
    "electron-builder": "26.12.0",
    "typescript": "5.9.3"
  },
  "build": {
    "appId": "com.opencodex.desktop",
    "productName": "OpenCodex",
    "asar": true,
    "files": [
      "dist/**/*",
      "package.json",
      "!dist/builder-debug.yml",
      "!resources/**/*"
    ],
    "extraResources": [
      {
        "from": "resources/staging/opencodex",
        "to": "opencodex"
      },
      {
        "from": "resources/staging/opencodex/node_modules",
        "to": "opencodex/node_modules",
        "filter": ["**/*", "!bun{,/**/*}", "!@oven{,/**/*}"]
      },
      {
        "from": "../src/tray/assets",
        "to": "tray"
      }
    ],
    "directories": {
      "output": "out"
    },
    "win": {
      "target": [
        { "target": "nsis", "arch": ["x64"] },
        { "target": "portable", "arch": ["x64"] }
      ],
      "artifactName": "OpenCodex-${version}-${arch}.${ext}",
      "requestedExecutionLevel": "asInvoker",
      "verifyUpdateCodeSignature": false
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "selectPerMachineByDefault": true,
      "allowToChangeInstallationDirectory": false,
      "include": "build/installer.nsh",
      "unicode": true,
      "createDesktopShortcut": "always",
      "createStartMenuShortcut": true,
      "menuCategory": "OpenCodex",
      "runAfterFinish": false,
      "deleteAppDataOnUninstall": false,
      "artifactName": "OpenCodex-Setup-x64.${ext}"
    },
    "portable": {
      "artifactName": "OpenCodex-Portable-x64.${ext}"
    }
  }
}
