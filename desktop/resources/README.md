# Desktop resources

This directory is generated by `bun run package:resources` from the desktop
project. The command stages the GUI build, backend source/metadata, production
dependencies, and the fixed Bun 1.3.14 Windows runtime under
`resources/staging/opencodex`; it also writes a sorted `manifest.json` for the
package audit. Generated staging and Electron output are ignored by Git.
