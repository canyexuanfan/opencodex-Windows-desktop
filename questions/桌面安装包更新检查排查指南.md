# 桌面安装包更新检查排查指南

## 问题描述

桌面端“检查更新”不能继续沿用 CLI/npm 包更新逻辑。桌面用户安装的是 Windows 安装包，检查更新应面向用户 fork 仓库的 GitHub Releases，并识别可下载的 Windows Setup 资产。

## 已尝试的修复方法及失败原因

- ❌ 2026-08-02：只把 `src/update/job.ts` 的 Release notes URL 改为 `https://github.com/canyexuanfan/opencodex-Windows-desktop/releases/latest`。这只能改变展示链接，实际版本查询仍然调用 `npm view @bitkyc08/opencodex`，更新执行仍然是 CLI 包安装；桌面端安装包更新检查并没有真正完成。

## 深层问题分析

桌面端存在两套版本语义：

- 后端代理包版本：根目录 `package.json`，例如仪表盘显示的代理版本。
- Electron 安装包版本：`desktop/package.json`，对应 Windows 安装器版本和桌面壳版本。

桌面安装包更新应使用 Electron 安装包版本比较 GitHub Release tag/资产，而不是使用 npm registry 上的 CLI 包版本。

## 下一步排查策略

1. 让 Electron 主进程启动 sidecar 时写入桌面模式和桌面安装包版本环境变量。
2. `/api/update/check` 在桌面模式下请求 fork 仓库 GitHub Releases。
3. 解析最新 release 的 Windows Setup 资产，提供下载入口；找不到资产时明确提示，而不是回退到 npm。
4. CLI/npm 安装场景继续使用原有自更新流程。

## 调试工具

- `rg "OPENCODEX_DESKTOP|api.github.com/repos|OpenCodex-Setup-x64" src desktop gui tests`
- `bun test tests/update-job.test.ts`
- `cd desktop && bun test tests/package-static.test.ts`

## 注意事项

- 不要在用户正在使用 Codex 时自动运行安装包覆盖自身。
- 不要把桌面安装包更新按钮伪装成 CLI 自更新。
- GitHub Release API 失败、缺少 Windows 安装包资产时，必须给用户可理解的状态。

## 更新记录

- ❌ 2026-08-02：确认当前更新链只换了 release notes 链接，未完成桌面安装包更新检查。
- ❌ 2026-08-02：阶段 30 虽然已将桌面更新检查切到 fork GitHub Releases，但桌面壳版本仍是 `0.1.0`，根包/代理版本是 `2.8.0`，会造成桌面壳、代理和 Release tag 的版本语义割裂。曾短暂考虑让安装包资产名携带版本号，但用户最终明确要求普通发布链只认固定安装版资产 `OpenCodex-Setup-x64.exe`；下一步应把桌面版本对齐根包版本，同时保持发布资产名固定，版本信息放在应用版本与 GitHub Release tag 中。
- ❌ 2026-08-02：按“只发布安装版”策略补充测试后，在默认沙箱中运行 `bun test tests/desktop-release-update.test.ts tests/release-notes.test.ts tests/update-job.test.ts`，新增 Release 解析与文案测试通过，但既有 `tests/update-job.test.ts` 后半段再次在 `%TEMP%\ocx-update-job-13248-*` 目录发生 `EPERM/EACCES rename/rm/lstat` 级联失败。该失败仍属于 Windows 沙箱临时目录/句柄限制；下一步使用非沙箱权限复跑 focused 套件，并清理这次残留的临时目录。
- ❌ 2026-08-02：阶段 31 重新生成桌面资源时，在默认沙箱中创建 `C:\tmp\opencodex-resource-stage31-20260802` 被 PowerShell 报 `UnauthorizedAccessException` 拒绝，随后 `bun run package:resources` 仍因 `AccessDenied accessing temporary directory` 失败。该路径属于本轮精确构建临时目录，不是生产代码问题；下一步用非沙箱权限创建同一精确目录并复跑资源打包。
- ✅ 2026-08-02：使用非沙箱权限复跑 `bun test tests/desktop-release-update.test.ts tests/release-notes.test.ts tests/update-job.test.ts`，62/62 通过、171 个断言通过；随后精确删除 19 个 `%TEMP%\ocx-update-job-13248-*` 残留目录，复核结果为 `NO_REMAINING_OCX_UPDATE_JOB_13248_DIRS`。
- ✅ 2026-08-02：按用户最终策略完成阶段 31：桌面壳版本对齐根包 `2.8.0`，electron-builder 只保留 x64 NSIS 安装版，发布资产固定为 `OpenCodex-Setup-x64.exe`；更新检查只匹配该资产，README/Release notes 只引导安装版，portable 保留为未来 `advanced/portable` 单独构建。最终 Setup SHA-256=`2C0423D557BFFC9F4627C8ED8E8C0E6C0F198E54AEEE5F5407D0F4CA93E5DBF0`。
- ❌ 2026-08-02：默认沙箱中运行 `bun test tests/update-job.test.ts tests/desktop-release-update.test.ts` 时，新加的桌面 release 解析与桌面 runtime 检查用例先通过，但既有 `tests/update-job.test.ts` 后半段在 `%TEMP%\ocx-update-job-*` 目录发生 `EPERM/EACCES rename/rm/lstat` 级联失败。该类失败已在既有排查记录中确认属于 Windows 沙箱临时目录/句柄限制；下一步改用非沙箱或 bundled Bun 复跑同一 focused 套件，不能据此修改生产代码。
- ✅ 2026-08-02：使用非沙箱权限重跑 `bun test tests/update-job.test.ts tests/desktop-release-update.test.ts`，39/39 通过、128 个断言通过。确认桌面 runtime 会检查 fork GitHub Release 安装包，不调用 npm registry；缺失 Windows Setup 资产会明确返回 `desktop_asset_missing`。
- ❌ 2026-08-02：重建桌面资源时直接运行 `cd desktop && bun run package:resources`，内部生产依赖安装报 `AccessDenied accessing temporary directory. Please set $BUN_TMPDIR or $BUN_INSTALL`。该失败与既有 Bun/Windows 沙箱临时目录限制同类；下一步使用仓库 `.tmp` 精确隔离目录，同时设置 `TEMP/TMP/BUN_TMPDIR/BUN_INSTALL` 后重试。
- ❌ 2026-08-02：将 `TEMP/TMP/BUN_TMPDIR/BUN_INSTALL` 指向仓库 `.tmp\desktop-resource-stage30-20260802` 后，`bun run package:resources` 未再立即报权限错误，但 300 秒内停留在生产依赖安装开始，未生成 `desktop/resources/staging/manifest.json`。检查未发现新的仓库 Bun 残留进程；下一步改用已授权的 `C:\tmp` 精确临时根重跑，避免工作区 `.tmp` ACL/句柄干扰。
- ✅ 2026-08-02：改用已授权的 `C:\tmp\opencodex-resource-stage30-20260802` 精确临时根后，`bun run package:resources` 成功生成 8174 个 staging 文件；`manifest.json` 与 `win-unpacked` 均确认包含 `src/update/desktop-release.ts`、`OPENCODEX_DESKTOP_VERSION` 和 fork Release API。electron-builder 成功生成新 NSIS/Portable；构建临时目录已清理。
- ✅ 2026-08-02：经用户授权，精确删除默认沙箱失败留在 `%TEMP%` 下的 19 个 `ocx-update-job-30000-*` 临时测试目录，复核结果为 `NO_REMAINING_OCX_UPDATE_JOB_30000_DIRS`。
- ✅ 2026-08-02：补上无 tag 默认通道由桌面安装包版本决定后，重新生成资源树与安装包；`win-unpacked` 确认包含 `checkForUpdateForRuntime(rawTag ? normalizeUpdateChannel(rawTag) : undefined)`。最终 Setup SHA-256=`7D39DFEB8D68377A2689C53BF283A7441DF880B0AE5CB09A30195423B3D3316E`，Portable SHA-256=`A272B60BCBBF942A7AFC595FCFB800E594F2603BEAB022F0FBFC07A6F04A5E52`。
