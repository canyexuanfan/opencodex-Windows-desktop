# 上游 v2.22.0 同步合并排查指南

## 问题描述

本 fork 基于 v2.8.0 合并基点，桌面端改造以补丁方式同步过 v2.10.0；上游 lidge-jun/opencodex
已推进到 v2.22.0（约 6280 个非 merge 提交、18 个正式版本）。直接 merge 产生 216 个冲突。

## 已尝试的修复方法及失败原因

- ❌ `git log --oneline f9b9440c..upstream/main` 初次用错比较对象（f9b9440c 是本地的
  upstream/v2.10.0 远程分支引用），应为合并基点 v2.8.0（1adad35）。
- ❌ 首次 `git add docs questions` 失败：仓库启用了 sparse-checkout，`questions/` 不在
  sparse 列表内，需要 `git add --sparse`。
- ❌ 全量 `bun run test` 第一次运行后 Bun 1.3.14 崩溃（internal assertion，exit 3），
  属 Bun 自身 bug（RSS ~1GB / Commit 1.5GB 时），非测试断言失败；需分块或重跑确认。
- ❌ 后台任务通过 grep 管道过滤测试输出，任务被中断时缓冲未刷盘，日志为空；
  改为直接重定向到文件。

## 成功方法（✅ 已验证）

- ✅ 冲突分诊：先算出「本地真实改动文件集合」（排除两个同步补丁提交 cce834ddd/23cc3cb2e
  涉及的文件），其余 168 个冲突直接 `git checkout --theirs` 取上游。
- ✅ 48 个本地真实改动文件逐个处理：
  - 内存治理/turn 租约类（上游已有更完整实现）→ 取上游；
  - 桌面端专属（App.tsx 桌面启停、.gitignore、README、styles.css）→ 手工合并双方；
  - i18n 文案冲突（本地旧文案 vs 上游重写）→ 取上游。
- ✅ 三个坏自动合并（双方各自追加了同名函数）：kiro-tools.ts、catalog/aggregation.ts、
  oauth-account-routes.ts 出现 TS2393 重复实现 → 直接恢复上游版本解决。
- ✅ 桌面宿主依赖的服务端接口重放：
  - `startServer` 的 `hostname` 选项 + `/healthz` buildRevision（src/server/index.ts）；
  - codex/sync 关机闸门（blockCodexSyncsForShutdown 等，按上游新 CodexSyncResult 结构重写）。
- ✅ 本地优化核对结论：
  - SSE 16MiB 缓冲上限（7d354ab）：上游 v2.22 已有 BoundedSseFrameBuffer /
    MAX_INSPECTION_SSE_FRAME_BYTES / translatorBudget 完整体系，无需重放；
  - WS turn 取消链（3a285887）：上游仍是裸 cancel，已重放（d4091c78e）；
  - turn 租约/HTTP fallback admission：上游 runAdmittedHttpTurn + isTransferred 更完整，取上游。
- ✅ 桌面安装包更新机制重放到上游新更新架构（bfb782c98）：
  Installer 加 "desktop"、checkForUpdateForRuntime、runGuiUpdateWorker 桌面分支、
  config-routes 两个端点改运行时感知。
- ✅ `bun x tsc --noEmit` 通过。

## 深层问题分析

- 本 fork 的「同步」历史是补丁式（squash/cherry-pick），git 无法识别同源改动，
  导致冲突面被放大到 216 个文件；后续同步建议改用真 merge 基线（本次已建立）。
- Bun 1.3.14 在 Windows 上跑 803 个测试文件的长套件会内崩，--isolate 模式下内存
  峰值高；分块运行或接受 exit 3 但统计 pass/fail 是当前可行路径。

## 下一步排查策略

- 全量测试结果统计（进行中）；若仅 Bun 崩溃无真实失败，记录后继续桌面打包验证。
- 桌面端 electron-builder 打包冒烟：gui 构建 + desktop 打包链路。

## 调试工具

- `git merge-base HEAD upstream/main`、`git diff --name-only --diff-filter=U`
- `comm -12 local_only.txt conflicts.txt`（本地真实改动 ∩ 冲突）
- `bun x tsc --noEmit`、`bun test --isolate <子目录>`

## 注意事项

- `git add` 需要 `--sparse`；`questions/`、`desktop/` 不在上游 sparse 列表。
- 上游 remote 地址是裸 URL（remote 名缺失时用 `git fetch <url> main:refs/remotes/upstream/main`）。

## 更新记录

- 2026-08-18：创建；记录 216 冲突解法、三个坏自动合并、Bun 崩溃与日志坑。
- 2026-08-19：✅ 验证收尾：
  - `bun x tsc --noEmit` 通过；`bun run build:gui` + `bun run lint:gui`（oxlint 0 警告）通过。
  - i18n 修复：en/ko 被 Python 批量解冲突时产生 85 个重复键（TS1117），
    ✅ 成功方法：以上游文件为基底重写，再把本 fork 的 13 个桌面键
    （dash.start / dash.updateDesktopInstaller 等）插回 `} as const;` 之前；
    fr/tr/zh-TW 补入对应语言翻译，zh-TW 用繁体、fr/tr 人工翻译。
  - 全量测试结论：本机 Bun 1.3.14 跑 803 个文件必然崩溃（exit 3，internal
    assertion），且纯净 upstream/main 同样崩溃、超时失败更多（181 vs 合并树更少），
    失败均为 5-8s 的 Windows schtasks/网络探测超时 —— 环境问题，非合并引入。
  - 聚焦验证（改动子系统）：ws-endpoint / passthrough-abort / shutdown-drain /
    update-job / codex-sync-api / cli-restore-back，125 pass / 3 fail，
    3 个失败在纯净上游完全一致（codex-sync-api 两个 + cli-restore-back 一个，
    均为真实进程探测类测试）。
  - 关键经验：跑全量必须用 `bun run test`（scripts/test.ts 包装器提供隔离 HOME），
    裸 `bun test --isolate ./tests/` 会在 active-registry-admission 处跨文件卡死；
    残留的昨日 bun 测试进程会把套件拖到 26 分钟级（包装器注释里已有警告），
    跑前先 `Get-CimInstance Win32_Process -Filter "Name='bun.exe'"` 清点。
