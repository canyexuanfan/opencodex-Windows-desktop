# 阶段 0 基线依赖缺失排查指南

## 问题描述

阶段 0 基线命令在干净工作区执行时，根项目与 `gui/` 均没有安装依赖，导致类型检查、GUI lint/build 和大量测试无法加载依赖。

## 已尝试的修复方法及失败原因

- ❌ `bun run typecheck`：退出码 1，`Cannot find type definition file for 'bun-types'`。工作区没有安装根依赖。
- ❌ `bun run test`：退出码 1。除缺少 `zod/v4`、`@bufbuild/protobuf`、React 等依赖外，当前 Bun 1.2.20 的 `Bun.Image` 为 undefined，相关图像测试无法运行。
- ❌ `cd gui && bun run lint`：退出码 1，`eslint` 不存在。GUI 依赖未安装。
- ❌ `cd gui && bun run build`：退出码 1，`tsc` 不存在。GUI 依赖未安装。
- ❌ 首次 `bun install --frozen-lockfile`：命令超过 120 秒被终止；随后检查确认根依赖已落地，未产生受跟踪文件改动。GUI 安装在单独目录完成。
- ❌ 安装依赖后 `bun run typecheck`：退出码 1，现有 `src/adapters/cursor/native-exec-common.ts` 判别联合类型错误，以及 `src/server/index.ts` 的 Buffer/ArrayBuffer 类型错误；未修改产品代码。
- ❌ 安装依赖后 `cd gui && bun test tests`：退出码 1，437 个测试中 430 通过、7 个 Logs 相关测试失败；失败集中在既有日志轮询/状态测试，未修改产品代码。
- ❌ 阶段 5 复跑 `cd gui && bun test tests`：退出码 1，439 个测试中 432 通过、同一组 7 个 Logs 相关测试失败；错误仍为测试使用的 `jest.advanceTimersByTime` 在当前 Bun 测试兼容层中不存在，未修改非相关日志代码。
- ❌ 阶段 6 首次 `bun run package`：electron-builder 拒绝将 Electron 放在运行时 `dependencies`，并在失败构建后发现旧的 `dist/win-unpacked` 可能被下一次 `dist/**/*` 收入 asar；已修正依赖分区、构建输出清理和资源排除规则。
- ❌ 阶段 7 使用自签名证书执行 `signtool verify /pa`：签名本体已存在，但未受信任根导致链验证失败；临时导入 CurrentUser\\Root 后 Windows Authenticode 查询在本机时间戳/链校验阶段超时，随后确认临时根未留存。该限制不影响签名文件生成，但信任链验证需在测试机完成。
- ❌ 阶段 8 复跑 `bun run test`：根类型检查先通过，但完整测试在 180 秒上限内无结果并被终止；仅发现 `scripts/test.ts`/`bun test --isolate` 进程，已按精确命令行停止，未修改生产代码。既有 Bun 测试运行器不稳定限制继续保留。
- ❌ 阶段 8 产物扫描首次发现 `src/AGENTS.md` 被复制到运行资源；已修正资源复制过滤器，重新生成 4259 文件资源、重新构建并重新签名，随后内部路径和私钥扫描通过。
- ❌ 2026-08-01 在无额外授权下将根完整测试上限提高到 420 秒：测试运行器输出大量业务测试通过，但多个测试在清理临时配置目录时收到 `EPERM/EACCES`，CLI 子进程另有 5 秒超时，最终仍以 420 秒超时退出；症状集中于当前 Windows 沙箱的临时目录/子进程权限，不能据此修改生产代码。
- ✅ 安装依赖后 `cd gui && bun run lint`：通过。
- ✅ 安装依赖后 `cd gui && bun run build`：通过；Vite 仅报告 bundle 大小提示。
- ✅ 阶段 6 `bun run package:resources`：使用锁定 root 依赖生成 4260 文件资源清单，包含 GUI、src、生产依赖和 98 MB Bun 1.3.14 runtime；实际扫描未发现 `.git`、questions、reference、todolist 或证书/密钥后缀。
- ✅ 阶段 6 `bun run package`：生成 `OpenCodex-Setup-x64.exe` 与 `OpenCodex-Portable-x64.exe`；Electron-builder 使用 per-user NSIS/asInvoker 配置，app.asar 不再包含 staging 或旧 unpacked 输出，固定 Bun 位于 unpacked resources/opencodex/runtime。
- ❌ `git clone --depth 1 --filter=blob:none https://github.com/electron/packager.git reference/electron-packager`：退出码 128，Windows 环境 OpenSSL `SSL_ERROR_SYSCALL`；已在参考索引中记录，未重复扩张检索范围。
- ✅ `bun run privacy:scan`：通过。
- ✅ `git fsck --connectivity-only`：通过；仅有两个 dangling tree，无连通性错误。

## 深层问题分析

当前终端使用 Bun 1.2.20，而根 `package.json` 锁定的 Bun 依赖为 1.3.14；安装依赖后仍需确认运行器是否满足项目测试对 Bun 图像 API 的要求。缺失依赖属于环境准备问题，不应通过修改产品代码绕过。

## 下一步排查策略

1. 使用根与 GUI 各自的锁文件安装依赖，优先采用 frozen lockfile。
2. 记录 `bun --version`，确认安装结果与锁定版本关系。
3. 重新运行失败的类型检查、测试、lint、build。
4. 若仅因当前 Bun 运行器缺少 `Bun.Image` 仍失败，保留该环境限制并继续不改产品代码。

## 调试工具

- `bun --version`
- `bun install --frozen-lockfile`
- `bun run typecheck`
- `bun run test`
- `cd gui && bun test tests`
- `cd gui && bun run lint`
- `cd gui && bun run build`

## 注意事项

- 不修改根 `bun.lock`，除非 frozen install 明确证明锁文件损坏。
- 不手改 `gui/dist`。
- 不为基线测试临时替换生产依赖或屏蔽失败测试。

## 更新记录

- 2026-08-01：记录阶段 0 首次基线失败，待安装锁定依赖后复验。
- 2026-08-01：阶段 1 首次 `desktop/bun install` 超过 300 秒超时，未产生 `desktop/node_modules`；疑似 Electron 依赖下载/安装脚本网络阻塞，待用 `--ignore-scripts` 复验。
- 2026-08-01：`desktop/bun install --ignore-scripts` 同样未在 180 秒内返回，但已落地完整依赖目录；Electron 38.3.0 的 `dist/electron.exe` 尚未下载，因此只进行 TypeScript 静态编译和 Bun 测试，未宣称 Electron 运行时 smoke 通过。
- 2026-08-01：阶段 1 验证成功：`cd desktop && bun run typecheck`、`bun run build`、`bun test tests` 全部通过（2 tests）；根 `bun.lock` 未变化。依赖版本在 `desktop/package.json` 中精确锁定，Electron 的 `install.js` 已审查，后续安装/打包阶段需恢复正常 postinstall 下载。
- 2026-08-01：依赖安全复核发现 Electron 38.3.0 低于公开修复线 38.8.6 且 38.x 已结束支持；已将 `desktop/package.json` 更新为 Electron 43.2.0 与 electron-builder 26.12.0。当前本地静态编译仍使用已缓存的旧 Electron 类型，下一次依赖完整安装后必须复跑版本与打包检查。
- 2026-08-01：阶段 2 复跑 `bun run test`（根项目 bundled Bun 1.3.14）在运行约 72 秒、RSS 约 0.68GB 后触发 Bun internal assertion，退出码 3；日志位于 `.tmp/phase2-root-test.log`（`.tmp/` 已忽略）。这是运行器崩溃而非测试断言失败，未修改产品代码；阶段 2 改动的根 typecheck 与 focused server/ready tests 均通过。
- 2026-08-01：阶段 1 依赖安装过程中生成过一份旧版本 `desktop/bun.lock`，在 Electron 版本升级后尝试用 `bun install --lockfile-only --ignore-scripts` 重生成仍长时间无输出；为避免提交与 `desktop/package.json` 不一致的锁文件，已删除该陈旧生成物。当前版本精确约束以 `desktop/package.json` 为准，后续具备稳定网络后必须重新生成并 frozen 校验桌面锁文件。
- 2026-08-01：使用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ bun install` 仍在 300 秒后超时；本次已成功生成与 `desktop/package.json` 一致的 `desktop/bun.lock`（Electron 43.2.0 / electron-builder 26.12.0），但 `electron/dist/electron.exe` 仍不存在，真实 Electron smoke 继续受网络下载阻塞。
- 2026-08-01：从同一镜像直接下载 Electron 43.2.0 Windows x64 ZIP 并解压到被忽略的本地 `desktop/node_modules/electron/dist` 后，`electron.exe --version` 返回 `v43.2.0`；真实双启动 smoke 通过：第一次主进程 1、sidecar wrapper/worker 共 2 个，第二次启动仍主进程 1、sidecar 仍 2 个，结束后本次 smoke 进程均已按命令行核验并停止。
- 2026-08-01：`cd desktop && bun audit --audit-level high` 返回 404（Bun audit 服务响应不可用）；已保留 package license/install-script 人工审查与公开 Electron 安全版本核对，后续 CI/联网环境需补一次依赖审计。
- 2026-08-01：阶段 4 sidecar 生命周期 smoke 通过：临时配置根启动后 `runtime-port.json` 存在，向受控 stdin 写入 `stop` 后进程正常退出且 runtime 文件消失；未使用未知 PID 强杀。
- 2026-08-01：阶段 5 完整 GUI 测试复跑仍复现同一 7 个 Logs 测试失败；新增桌面生命周期定向测试通过，未将既有 Bun/Jest 兼容性失败误归因于本次 GUI 改动。
- 2026-08-01：阶段 6 修复 electron-builder 依赖分区与旧输出污染后，安装版/便携版均成功构建；在 PATH 仅保留 `C:\Windows\System32` 的打包 Electron smoke 中，主进程成功拉起 resources 内 Bun sidecar，未发现外部 Bun/Node 依赖；强杀 smoke 仅使用隔离 `.tmp` 配置根，遗留 runtime 文件属于异常退出恢复场景。
- 2026-08-01：阶段 7 生成 CN=十七°、RSA 3072、SHA-256、代码签名 EKU 证书，electron-builder 将安装器、便携版、Electron 主 EXE 与内置 Bun 均写入签名；Get-AuthenticodeSignature 能读取签名主体，当前机未完成受信根后的 Valid/signtool chain 验证，保留为发布前测试机事项。
- 2026-08-01：阶段 8 根 `bun run test` 在 180 秒内无输出并超时；根 typecheck 已通过，桌面 9 tests、GUI 定向测试及 GUI lint/build 仍作为独立回归证据保留。
- 2026-08-01：修正运行包排除 `AGENTS.md` 后，最新签名 thumbprint=4D0D7BD4C925CEBE985B25F97776337536D064CB；安装器 SHA-256=9E9BB84C61C820D27D7D0A3CC58A85D9FE5E410799762B4870E6CCCE9836707B，便携版 SHA-256=0A33A6BEACDE98D1AEFF1C161FA4EE049AFAFF389B86BF50EC77E6E1DC919A15；四个签名目标主体均为 CN=十七°。
- 2026-08-01：根完整测试在无额外授权环境下仍受临时目录清理权限和子进程超时影响；下一步仅复核一次最小授权运行，若仍失败则保留为环境限制。
- ❌ 2026-08-01 最小授权重跑根完整测试：420 秒后仍无完成结果，命令返回超时；未获得可证明的全套通过结果。
- ❌ 2026-08-01 在并行快速回归中重跑核心 focused tests：`desktop-ready` 与 loopback 断言通过，但 `server-auth` 因临时目录 `lstat/rm` 收到 `EPERM/EACCES`，导致 43 个级联失败和 87 个清理错误；与前述 Windows 沙箱文件权限问题同类，不修改认证/服务器代码。
- ✅ 2026-08-01：超时后使用低权限 `Get-Process` 按本次运行的启动时间/路径清理 6 个 Bun 子进程，确认无残留；不修改生产代码。
