name: Maintainer port PR 1039

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Port completed; this one-shot workflow is disabled."
