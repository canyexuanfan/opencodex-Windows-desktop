# 020 — Wave 1: #1805, #1806, close #1786

## 020.1 #1805 (no code change needed)

Head `8946a1026120c59870310575c1623873a3660f4f`. `scripts/test.ts` creates `AppData/Local` and `AppData/Roaming` inside the disposable sandbox root before exporting `USERPROFILE`; `tests/test-runner.test.ts` gains two Windows-only regressions, one of which drives the actual PowerShell/.NET known-folder expression.

Disposition: merge as-is. Production runtime untouched.

## 020.2 #1806 blocker fix

Head `960013e312970494e6eb7a3209b33491627f4316`. `src/service.ts` change is correct: `currentCodexSqliteHomeAbsolute()` becomes target-aware and uses `path.posix` for native/POSIX targets, so `/var/lib/opencodex/codex-sqlite` survives generation on a Windows host.

Blocker is test-only, at `tests/service.test.ts:692` in the case named "still absolutizes a relative sqlite home". It asserts the key exists and that the exact raw relative string is absent. That passes for any non-empty transformed value, absolute or not.

Required diff (test file only):

```diff
-  expect(unit).toContain("CODEX_SQLITE_HOME");
-  expect(unit).not.toContain("Environment=CODEX_SQLITE_HOME=relative/sqlite");
+  const line = unit.split("\n").find(l => l.includes("CODEX_SQLITE_HOME="));
+  expect(line).toBeDefined();
+  const value = line!.split("CODEX_SQLITE_HOME=")[1]!.trim();
+  expect(value.length).toBeGreaterThan(0);
+  expect(isAbsolute(value) || posix.isAbsolute(value) || win32.isAbsolute(value)).toBe(true);
+  expect(value.endsWith("relative/sqlite") || value.endsWith("relative\\sqlite")).toBe(true);
```

Apply the same shape to the launchd variant if it shares the weak oracle. Import `isAbsolute` plus `posix`/`win32` from `node:path` at the top of the test file.

## 020.3 Merge and close

1. Merge `#1805` into `dev`.
2. Land the strengthened `#1806` (either push the test fix onto the PR head branch or fold it into the dev-side merge commit) and merge.
3. Run `bun test tests/service.test.ts tests/test-runner.test.ts` focused evidence.
4. Close `#1786` with a comment naming the merged commit and stating release is pending.
