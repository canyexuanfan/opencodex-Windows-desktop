# 桌面打包临时目录权限排查指南

## 问题描述

在 Windows/Codex 沙箱环境中重新构建 OpenCodex 桌面安装包时，`desktop` 包的 `package:resources` 阶段需要调用 Bun 安装生产依赖并复制资源。如果 Bun 默认临时目录不可访问，会报 `AccessDenied accessing temporary directory. Please set $BUN_TMPDIR or $BUN_INSTALL`，导致安装包没有生成。

## 已尝试的修复方法及失败原因

- ❌ 2026-08-02：用户安装并重启 Codex 后复核，当前 `18313/healthz` 正常，但 `~/.opencodex/config.json` 的 `port` 仍为 `10100`，安装目录中的 `src/update/job.ts`/`src/update/index.ts` 未包含 `git-v0.58` 的端口迁移函数，说明用户安装的是最终收口前的安装包。随后尝试用当前仓库执行 `cd desktop && bun run package`，在 `package:resources` 阶段因 Bun 报 `AccessDenied accessing temporary directory. Please set $BUN_TMPDIR or $BUN_INSTALL` 失败。该失败与既有 Windows 沙箱临时目录权限问题同类，不是端口迁移业务逻辑失败。

## 深层问题分析

- Bun 在安装依赖或准备资源时会使用默认临时目录；当前沙箱/Windows ACL 组合可能拒绝访问该目录。
- 端口迁移代码已经在仓库 `git-v0.58`，但用户当前安装目录并未包含最终收口代码，所以需要重新打包一个基于当前工作树的安装包。
- 构建安装包本身不应停止、重启或覆盖安装正在运行的 Codex/OpenCodex；只生成产物，后续安装交给用户手动执行。

## 下一步排查策略

1. 使用明确的 `C:\tmp\opencodex-desktop-package-20260802` 作为 `TEMP`/`TMP`/`BUN_TMPDIR`。
2. 如普通沙箱仍拒绝，使用非沙箱权限只执行构建命令。
3. 构建完成后检查安装包名称、哈希、安装目录 staging 中是否包含 `selectRestartPortAfterReclaim`/`selectPostUpdatePort` 等端口迁移函数。

## 调试工具

- `bun run build:gui`
- `cd desktop && bun run package`
- `Select-String -Path desktop/resources/staging/opencodex/src/update/*.ts -Pattern "selectRestartPortAfterReclaim|selectPostUpdatePort"`
- `Get-FileHash desktop/out/OpenCodex-Setup-2.8.0-x64.exe -Algorithm SHA256`

## 注意事项

- 不要自动运行安装包。
- 不要重启 Codex 或停止当前支撑对话的代理。
- 如果只剩安装/重启验证，交还用户手动操作。

## 更新记录

- ❌ 2026-08-02：首次记录当前打包失败，下一步切到明确 `C:\tmp` 临时目录并重试。
- ✅ 2026-08-02：使用非沙箱权限与明确临时根 `C:\tmp\opencodex-desktop-package-20260802` 重新执行 `cd desktop && bun run package` 成功，生成 `desktop/out/OpenCodex-Setup-2.8.0-x64.exe`。产物验收确认 `desktop/resources/staging/opencodex/src/update/job.ts` 包含 `selectRestartPortAfterReclaim`，`src/update/index.ts` 包含 `selectPostUpdatePort`，`src/server/ports.ts` 包含 `shouldPersistSelectedPort`；`desktop/out` 中没有 portable 产物。Setup SHA-256=`197519DCD6C0649D524E95766A504420FA65A70BF6760A173A491A0BBD39C302`。
- ❌ 2026-08-02：用户更新后继续验收，发现普通桌面启动仍走动态端口：Codex 路由与 `runtime-port.json` 为 `43337` 且健康，但 `~/.opencodex/config.json` 仍为 `10100`；安装目录已经包含更新路径端口迁移函数，说明遗漏点在 `src/desktop/entry.ts` 普通启动硬编码 `startServer(0)`。修复源码后尝试补签名重包，第一版 PowerShell 临时证书脚本使用 `[System.Security.Cryptography.RandomNumberGenerator]::Fill()`，当前运行时不支持该静态方法，命令在创建证书前失败。下一步改用兼容的 `RNGCryptoServiceProvider.GetBytes()`。
- ✅ 2026-08-02：改用 `RNGCryptoServiceProvider.GetBytes()` 生成临时 PFX 密码后，使用临时 `CN=十七°` CodeSigningCert 与 `CSC_LINK/CSC_KEY_PASSWORD` 重新执行 `bun run build:gui` 和 `cd desktop && bun run package` 成功。新安装包 `desktop/out/OpenCodex-Setup-2.8.0-x64.exe` SHA-256=`1812F1F2F6849868C5E212F5D579ACD500134B7B58725F404CC251F7999FEC27`；`Get-AuthenticodeSignature` 可读取安装包、unpacked `OpenCodex.exe` 与内置 `bun.exe` 的签名主体 `CN=十七°`，本机自签链状态为 `UnknownError`。产物同时包含普通桌面启动固定端口优先逻辑与 NSIS 桌面快捷方式总是创建逻辑；临时 PFX、临时证书和 `C:\tmp\opencodex-stage-sign-20260802c` / `C:\tmp\opencodex-desktop-package-20260802b` / `C:\tmp\opencodex-desktop-package-20260802c` 均已清理。
